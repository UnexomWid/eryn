#ifndef ERYN_DEF_WARNINGS_DXX_GUARD
#define ERYN_DEF_WARNINGS_DXX_GUARD

#ifdef _MSC_VER
    #pragma warning(disable: 4996)
#endif

// Suppress warnings generated when a variable is only used
// certain situations (e.g. exception data in debug mode).
#define IGNORE_UNREFERENCED(x) (void)x

#endif